
version: 2

sources:
  - name: tasty_bytes               # This is the "your_source" nickname
    database: DBT_TUTORIAL_DB
    schema: DBT_DATA_MODELING
    tables:
      - name: MENU                  # This must match the actual Snowflake table name

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
